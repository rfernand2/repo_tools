gh=cd /d %GITHUB_DIR%
tools=cd /d %GITHUB_DIR%\repo_tools
.data=cd /d %HOME%/.data
.models=cd /d %HOME%/.models
.xt=cd /d %USERPROFILE%/.xt
n=notepad $*
push=git add . && git commit -m $* && git push
repo=%CONDA3_DIR%\python %GITHUB_DIR%\repo_tools\repo\repo.py $* && call %GITHUB_DIR%\repo_commands.bat
rt=%CONDA3_DIR%\python %GITHUB_DIR%\repo_tools\repo\repo.py $* && call %GITHUB_DIR%\repo_commands.bat
